self.addEventListener('push',event=>{let data={};try{data=event.data?.json()||{}}catch{data={body:event.data?.text()||'Business & Life update'}};const title=data.title||'Business & Life';const attention=data.attention&&typeof data.attention==='object'?data.attention:{};const tag=[data.event_code,data.entity_type,data.entity_id].filter(Boolean).join(':')||'business-life:update';const options={body:data.body||'There is a new update.',tag,renotify:Boolean(attention.renotify),requireInteraction:Boolean(attention.requireInteraction),data:{url:data.url||'/'}};if(attention.silent===true)options.silent=true;else if(Array.isArray(attention.vibrate)&&attention.vibrate.length)options.vibrate=attention.vibrate;event.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate?.(url);return c.focus()}}return clients.openWindow(url)}))});
