# Preview pointer

This file exists only on the technical Railway preview pointer branch.

Purpose: trigger a normal GitHub push so the existing `accounting-preview` service can deploy the current Supplier V5 branch state for isolated QA while Railway's source-editing agent is unavailable.

Canonical product code remains on:
- `supplier/daily-operations-v5` before merge;
- `main` after merge.

Do not merge this preview-only file.
